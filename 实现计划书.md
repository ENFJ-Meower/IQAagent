# IQA Agent 框架 — 实现计划书

> Vibe Coding 指南：每一步告诉你对 AI 说什么、运行什么命令、如何肉眼验证。

---

## 前置准备

### 环境检查

```bash
python --version          # 需要 3.10+
pip install uv            # 推荐用 uv 管理包，装包更快
```

### 数据集准备（手动下载）

- KonIQ-10k val set → 解压到 `E:\Agent\data\koniq10k\`
  - 图片放 `images/`，MOS 标签文件放 `koniq10k_mos_with_names.csv`
- SPAQ test set → 解压到 `E:\Agent\data\spaq\`
  - 图片放 `images/`，标签文件放 `spaq_labels.csv`
- **KADID-10k** → 解压到 `E:\Agent\data\kadid10k\`（用于构建 AM 和校准，下载：https://database.mmsp-kn.de/kadid-10k-database.html）
  - 图片放 `images/`，标签文件放 `kadid10k_mos.csv`

**验证数据**：
```bash
ls E:\Agent\data\koniq10k\images | head -5   # 能看到 .jpg 文件
python -c "import pandas as pd; print(pd.read_csv('E:/Agent/data/koniq10k/koniq10k_mos_with_names.csv').head())"
```

### VLM 接入方式（二选一）

**选项 A — DashScope API（推荐先用）**：
```bash
pip install dashscope
export DASHSCOPE_API_KEY="你的key"
```
模型名：`qwen-vl-plus` 或 `qwen-vl-max`

**选项 B — 本地 Qwen2.5-VL-7B**：
```bash
pip install transformers accelerate torch
# 需要至少 16GB VRAM
```

---

## 阶段一：骨架搭建（预计 2-3 小时 vibe coding）

**目标**：整个项目目录结构建好，能对一张图片跑完完整流程，输出分数。

### Step 1.1 — 初始化项目结构

对 AI 说：
> 在 `E:\Agent` 下创建以下目录和空文件：`config/`、`skills/`、`tools/`、`router/`、`memory/`、`calibration/`、`evaluation/`，并创建 `requirements.txt`，内容包含：`opencv-python numpy scipy pillow pandas scikit-learn faiss-cpu`

**验证**：
```bash
ls E:\Agent
# 能看到 config/ skills/ tools/ router/ memory/ calibration/ evaluation/
```

---

### Step 1.2 — 安装依赖

```bash
cd E:\Agent
pip install opencv-python numpy scipy pillow pandas scikit-learn faiss-cpu
pip install piq   # 传统 IQA 工具（BRISQUE/NIQE）
```

**验证**：
```bash
python -c "import cv2, numpy, scipy, PIL, pandas, faiss, piq; print('all OK')"
```

---

### Step 1.3 — 写感知工具库

对 AI 说：
> 写 `E:\Agent\tools\perceptual_tools.py`，实现以下函数，输入都是图片路径（str），输出都是 numpy array 或 float：
> - `noise_residual_map(img_path)` → 图像减去高斯去噪版本，返回可视化图（numpy uint8）
> - `gradient_magnitude_map(img_path)` → Sobel 梯度幅度图，返回 numpy uint8
> - `fourier_magnitude_spectrum(img_path)` → 中心化 FFT 幅度谱，返回 numpy uint8
> - `luminance_histogram(img_path)` → 返回 256 bin 亮度直方图（numpy float array）
> - `gradient_sharpness_score(img_path)` → 返回 float，值越高越清晰（Laplacian 方差）
> - `noise_level_score(img_path)` → 返回 float，估计噪声标准差

**验证**：
```bash
python -c "
from tools.perceptual_tools import gradient_sharpness_score, noise_level_score
# 用 data 里任意一张图测试
score = gradient_sharpness_score('E:/Agent/data/koniq10k/images/xxx.jpg')
print('sharpness:', score)
"
```
肉眼检验：找一张明显模糊的图和一张清晰的图，模糊图的 `gradient_sharpness_score` 应该显著低于清晰图。

---

### Step 1.4 — 写传统 IQA 工具

对 AI 说：
> 写 `E:\Agent\tools\iqa_tools.py`，实现：
> - `brisque_score(img_path) → float`（用 `piq` 库，输出越低质量越好）
> - `niqe_score(img_path) → float`（同上）
> - `normalize_to_mos_scale(score, tool_name) → float`：用硬编码的线性映射把工具分映射到 1-5 MOS 尺度（BRISQUE 0-100 → 5-1，NIQE 0-20 → 5-1）

**验证**：
```bash
python -c "
from tools.iqa_tools import brisque_score
score = brisque_score('E:/Agent/data/koniq10k/images/xxx.jpg')
print('BRISQUE:', score)  # 一般 0-100，数越小越好
"
```
找几张明显好/差的图，验证 BRISQUE 分数方向对不对。

---

### Step 1.5 — 写 Prompt 模板

对 AI 说：
> 写 `E:\Agent\skills\prompts.py`，定义以下字符串常量：
> - `DISTORTION_DETECTION_PROMPT`：让 VLM 检测失真类型，从 7 类中选，返回 JSON
> - `DISTORTION_ANALYSIS_PROMPT`：让 VLM 评估每类失真严重程度（none/slight/moderate/severe/extreme），返回 JSON
> - `SCORING_PROMPT_WITH_EVIDENCE`：综合工具分数 + 失真分析结果，让 VLM 输出 1-5 分，返回 JSON
> - `COMPARATOR_PROMPT`：让 VLM 比较两张图的质量，回答 A 或 B
>
> 参考 `E:\Agent\需求书.md` 第 5 节的 Prompt 模板原文。

**验证**：
```bash
python -c "from skills.prompts import SCORING_PROMPT_WITH_EVIDENCE; print(SCORING_PROMPT_WITH_EVIDENCE[:200])"
```

---

### Step 1.6 — 写 VLM 适配层

对 AI 说：
> 写 `E:\Agent\skills\vlm_client.py`，实现：
> - `class VLMClient`，构造函数接受 `mode: str`（"api" 或 "local"）和 `model_name: str`
> - `def call(self, prompt: str, images: list[str] = None) -> str`：调用 VLM，返回文本响应
> - API 模式使用 DashScope，读取环境变量 `DASHSCOPE_API_KEY`
> - Local 模式用 `transformers` 加载 Qwen2.5-VL
> - 增加一个 `mock` 模式：不调用任何 VLM，直接返回硬编码的 JSON 字符串（用于测试流程）

**验证**（mock 模式）：
```bash
python -c "
from skills.vlm_client import VLMClient
client = VLMClient(mode='mock')
resp = client.call('test prompt')
print(resp)
"
```

---

### Step 1.7 — 写主 Pipeline

对 AI 说：
> 写 `E:\Agent\pipeline.py`，实现 `class IQAPipeline`：
> - 构造函数：接受 `vlm_client`，实例化工具和 Prompt
> - `def assess(self, img_path: str) -> dict`：按顺序执行 4 步
>   1. 运行感知工具（gradient_sharpness_score、noise_level_score、brisque_score）
>   2. 调用 VLM 做失真检测（解析 JSON，失败时返回空列表）
>   3. 调用 VLM 做失真分析（解析 JSON）
>   4. 调用 VLM 做综合打分（注入工具分数，解析 JSON，提取 quality_score）
> - 返回 dict：`{"score": float, "distortions": list, "reasoning": str, "tool_scores": dict}`
> - 每步 JSON 解析失败时打印警告，用 fallback 值继续

**验证**（用 mock VLM）：
```bash
python -c "
from pipeline import IQAPipeline
from skills.vlm_client import VLMClient
pipeline = IQAPipeline(VLMClient(mode='mock'))
result = pipeline.assess('E:/Agent/data/koniq10k/images/xxx.jpg')
print(result)
"
```
能看到包含 score / distortions / reasoning 的 dict 就成功。

---

### Step 1.8 — 写评测脚本

对 AI 说：
> 写 `E:\Agent\evaluation\run_eval.py`：
> - 读取 KonIQ-10k val set 的 MOS CSV（包含文件名和 MOS 列）
> - 对每张图运行 `IQAPipeline.assess()`，收集预测分
> - 计算并打印 SRCC 和 MAE
> - 支持命令行参数 `--dataset koniq|spaq --limit 50`（limit 用于快速测试前 N 张）
> - 结果保存到 `results/eval_{dataset}_{timestamp}.csv`

**验证**（只跑 5 张，mock VLM）：
```bash
python evaluation/run_eval.py --dataset koniq --limit 5
```
能看到 SRCC 和 MAE 输出（mock 模式下 SRCC 是随机的，不重要，主要验证流程不报错）。

---

**阶段一完成检查**：
- [ ] 5 张图的 pipeline 跑通无报错
- [ ] 工具分数方向正确（清晰图 > 模糊图）
- [ ] 评测脚本输出 SRCC 和 MAE

---

## 阶段二：接入真实 VLM + 获取 Baseline（预计 1-2 小时）

**目标**：用真实 VLM 跑 50-100 张图，获得 baseline SRCC，分析得分分布。

### Step 2.1 — 接入 API

```bash
export DASHSCOPE_API_KEY="你的key"
python evaluation/run_eval.py --dataset koniq --limit 50 --vlm api
```

预期等待时间：50 张图 × ~5 秒/张 = ~4 分钟。

**人工验证**：
1. 查看 `results/` 下生成的 CSV
2. 画分数分布直方图：
```bash
python -c "
import pandas as pd, matplotlib.pyplot as plt
df = pd.read_csv('results/eval_koniq_xxx.csv')
df['predicted_score'].hist(bins=20)
plt.savefig('results/score_dist.png')
print(df['predicted_score'].value_counts().head(10))
"
```
如果出现**得分坍缩**（大量图片集中在 3.0 / 4.0 / 5.0），说明 ME-IQA 是必要的。
记录 baseline SRCC，预期 ~0.45-0.60。

---

### Step 2.2 — 误差分析

对 AI 说：
> 写 `E:\Agent\evaluation\error_analysis.py`：
> - 读取评测结果 CSV 和 MOS 标签
> - 找出预测误差最大的 20 张图（|predicted - mos| 最大）
> - 找出排名误差最大的 20 对图（SRCC 贡献最差的）
> - 将这些图复制到 `results/hard_cases/` 文件夹

**人工验证**：
打开 `results/hard_cases/`，肉眼看这些图：
- 是什么类型的失真（噪声/模糊/压缩/曝光）？
- VLM 给的分数合理吗？
- 工具分数和 VLM 分数有没有冲突？

这步决定下一步怎么优化 Prompt。

---

## 阶段三：工具证据注入 VLM（预计 2-3 小时）

**目标**：把感知工具生成的图像（噪声残差图、梯度图）传给 VLM 看，解决 VLM 语义偏差。

### Step 3.1 — 生成并保存感知工具可视化

对 AI 说：
> 修改 `E:\Agent\tools\perceptual_tools.py`，给每个函数加一个 `save_path` 参数，可以把可视化图像保存到指定路径。

**人工验证**：
```bash
python -c "
from tools.perceptual_tools import noise_residual_map, gradient_magnitude_map
noise_residual_map('E:/Agent/data/koniq10k/images/xxx.jpg', save_path='test_noise.png')
gradient_magnitude_map('E:/Agent/data/koniq10k/images/xxx.jpg', save_path='test_grad.png')
"
```
打开生成的 png 文件，噪声残差图应该在高噪声区域更亮，梯度图在边缘区域更亮。

---

### Step 3.2 — 修改打分 Prompt 支持多图输入

对 AI 说：
> 修改 `E:\Agent\pipeline.py` 的 `assess` 方法：
> - 在 Step 4（综合打分）时，生成噪声残差图和梯度图保存到临时路径
> - 将原始图、噪声残差图、梯度图三张图一起传给 VLM（images 列表）
> - 修改 `SCORING_PROMPT_WITH_EVIDENCE` 告诉 VLM "第二张图是噪声残差图，第三张图是边缘梯度图，请参考这些客观感知证据来评分"

**验证**：
```bash
python -c "
from pipeline import IQAPipeline
from skills.vlm_client import VLMClient
pipeline = IQAPipeline(VLMClient(mode='api'))
result = pipeline.assess('E:/Agent/data/koniq10k/images/xxx_noisy.jpg')
print(result['reasoning'])  # 应该能看到 VLM 提到了噪声残差图
"
```
SRCC 对比：同样的 50 张图，加工具图 vs 不加，SRCC 应该有提升。

---

## 阶段四：Prompt 系统性优化（预计 2-3 小时）

**目标**：在纯 Skill + 工具证据框架内将 SRCC 推过 0.72。不引入任何外部数据库或检索机制。

**当前状态**（v0.1 baseline，50张）：SRCC=0.674，MAE=0.864

### 背景：为什么不做 ME-IQA 记忆重排序

任务书 §4.2 明确：**仅允许通过 Skill（Prompt）注入规则**。记忆重排序依赖外部图片库（无论来源），不属于 Skill，违反任务书约束。

---

### Step 4.1 — 误差模式分析

对 AI 说：
> 写 `E:\Agent\evaluation\error_analysis.py`：
> - 读取评测结果 CSV
> - 按照以下维度分组统计平均误差：
>   1. 按 predicted_score 区间（1-2, 2-3, 3-4, 4-5）
>   2. 按 distortions 类型
>   3. 找出预测比 MOS 高出超过 1.0 的图（系统性高估）
>   4. 找出预测比 MOS 低出超过 1.0 的图（系统性低估）
> - 输出统计报告，并将高估/低估的图复制到 `results/overest/` 和 `results/underest/`

```bash
python evaluation/error_analysis.py --results results/eval_koniq_v01_50imgs.csv
```

**人工验收**：打开 `results/overest/` 和 `results/underest/`，肉眼看这些图，找规律：
- 高估的图是什么类型？（内容好看但技术差？黑白？特殊光线？）
- 低估的图是什么类型？（内容普通但技术很好？）

---

### Step 4.2 — 针对高估模式改进 Prompt

根据误差分析结果，针对性修改 `skills/prompts.py`。

常见高估模式及对应修复方向：

| 高估模式 | 原因 | Prompt 修复方向 |
|---------|------|----------------|
| 黑白照得高分 | VLM 认为黑白是艺术风格 | 明确说明"黑白/去色本身是质量损失" |
| 夜景/暗光得高分 | VLM 认为暗调是氛围 | 明确说明"曝光不足是技术缺陷" |
| 人物/风景内容好得高分 | 语义偏差 | 强调"忽略内容，只看像素级质量" |
| 工具分数高但人工评分低 | 工具和人类标准不一致 | 调整工具权重，降低对 BRISQUE 的信任度 |

跑验证：
```bash
python -m evaluation.run_eval --dataset koniq --limit 50 --vlm api --output results/eval_koniq_v04_50imgs.csv
```

---

### Step 4.3 — 工具权重优化（Router 层，§4.3 允许）

任务书 §4.3 允许 Router 层对"评估规则的选择"进行优化。这意味着可以学习对不同失真类型选用不同工具权重。

对 AI 说：
> 修改 `E:\Agent\router\tool_selector.py`，实现根据检测到的失真类型动态调整工具权重：
> - Blur 为主：sharpness_mos 权重 0.6，brisque_mos 权重 0.4
> - Noise 为主：brisque_mos 权重 0.6，sharpness_mos 权重 0.4
> - Compression 为主：brisque_mos 权重 0.7，sharpness_mos 权重 0.3
> - 无明显失真：均等权重 0.5/0.5
> 将加权工具分传入 SCORING_PROMPT 替代原来的两个独立分数

---

### Step 4.4 — 增加局部感知证据（mix-grained）

Dog-IQA 的核心洞察：局部 patch 级质量 + 全局图像级质量融合比单纯全局评估更准确。

对 AI 说：
> 修改 `E:\Agent\pipeline.py`，在生成证据图时额外裁剪 4 个角落 patch（各 128×128 像素）：
> - 对每个 patch 计算 gradient_sharpness_score 和 noise_level_score
> - 取最差 patch 的分数作为"局部质量下限"
> - 将局部最差分和全局分一起传入 SCORING_PROMPT
> Prompt 中增加："Local patch quality (worst region): sharpness={local_sharp_mos:.2f}"

---

## 阶段五：离线 MAE 校准（✅ 已完成）

**目标**：用离线拟合的单调函数压缩 VLM 分数尺度，降低 MAE。SRCC 不变。

**合规性**：校准函数在 KonIQ-10k **train set** 上拟合，不接触 val/test 评测数据，符合 §4.1 和 §4.5。

### Step 5.1 ✅ — 校准脚本

`calibration/fit_calibrator.py` — 使用 Isotonic Regression（等调回归）而非 5 参数逻辑函数：
- 5 参数逻辑函数在小样本下非单调（会改变 SRCC），已放弃
- Isotonic Regression 数学上保证单调，SRCC 严格不受影响
- 输出保存为 200 点查找表（`calibration/params.json`）

```bash
# 在 train set 上拟合（100张）
python -m evaluation.run_eval --dataset koniq_train --limit 100 --vlm api --output results/eval_koniq_train_100.csv
python calibration/fit_calibrator.py --results results/eval_koniq_train_100.csv
```

实测：MAE 降幅 57%，SRCC 近似不变。

### Step 5.2 ✅ — 合规修复（v0.7）

发现并修复两处违规（详见修改日志 v0.7）：
- `iqa_tools.py`：归一化边界改为从 KonIQ **train set** 统计（p10/p90），不再用 val set
- 校准函数改为在 **train set** 上拟合

### Step 5.3 ✅ — 集成到评测脚本

`run_eval.py --calibrate` 加载 `calibration/params.json` 应用校准。

---

## 阶段六：全量评测 + 最终结果

### Step 6.1 — 跑完整 KonIQ val set

```bash
python -m evaluation.run_eval --dataset koniq --vlm api --calibrate --output results/eval_koniq_full.csv
```

预期：2016 张图，约 2-3 小时。可以分批跑（--limit 500，--limit 1000）。

### Step 6.2 — 跑 SPAQ test set（如果图片下载完整）

```bash
python -m evaluation.run_eval --dataset spaq --vlm api --calibrate
```

### Step 6.3 — 汇总结果

```
Stage                        KonIQ SRCC   KonIQ MAE
v0.1 Baseline                  0.6743       0.8635
v0.4 工具分锚定                 0.7372       0.6978
v0.5 Router 自适应权重          0.7530       0.6828
v0.6 局部 patch 分析            0.7552       0.7185
v0.6 + MAE 校准（train拟合）    0.7552       ~0.30
Full val set（最终）             TBD          TBD
```

---

## 各阶段预期进度与指标

| 阶段 | 完成标志 | 预期 KonIQ SRCC |
|------|---------|----------------|
| 阶段一 ✅ | pipeline 跑通 | N/A |
| 阶段二 ✅ | 真实 VLM baseline | 0.674（已达成） |
| 阶段三 ✅ | 工具证据注入 | 包含在 baseline 中 |
| 阶段四 ✅ | Prompt 优化 + Router 权重 + 局部 patch | 0.755（已达成，超目标 0.72）|
| 阶段五 ✅ | MAE 校准（train set 合规拟合）| SRCC 不变，MAE 降 57% |
| 阶段六 | 全量评测出最终数字 | — |

---

## 常见问题处理

**JSON 解析失败**：VLM 有时输出带 markdown 的 JSON，对 AI 说"帮我写一个 `extract_json(text)` 函数，能提取文本中第一个 JSON 块，支持去掉 ```json ... ``` 标记"

**API 限速报错**：在请求之间加 `time.sleep(1)`，或者对 AI 说"帮我加一个带指数退避的重试装饰器"

**内存不够**：AM 太大时，减少每个区间的采样数（40 → 20），K 从 16 改为 8

**SPAQ 标签格式不对**：先 `python -c "import pandas as pd; print(pd.read_csv('data/spaq/spaq_labels.csv').columns.tolist())"` 看看列名，再告诉 AI 实际的列名
