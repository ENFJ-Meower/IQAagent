# 图像质量评估 Agent 框架 — 产品需求文档（PRD）v2

> 基于 AgenticIQA / IQA-T1 / ME-IQA 三篇论文的深度分析，结合任务书约束重新设计。

---

## 1. 任务理解

### 1.1 核心目标

在不使用评测数据集（KonIQ-10k / SPAQ）训练或微调任何模型的前提下，通过 Skill（Prompt）与受限的 Router/Decision 机制，让 Qwen2.5-VL / Qwen3-VL 对图像质量打分，使预测分数与人类 MOS 的 **SRCC 尽可能高、MAE 尽可能低**。

### 1.2 关键约束（来自任务书）

| 约束 | 含义 |
|------|------|
| 不得微调 VLM | Backbone 权重冻结 |
| 不得用评测集训练工具模型 | 所有辅助模块均不能在 KonIQ/SPAQ 上训练 |
| 仅通过 Skill（Prompt）注入规则 | VLM 推理依赖 Prompt 工程 |
| Router/Decision 层可优化 | 规则选择、冲突裁决、解释生成 |
| MOS 仅离线使用 | 可用于构建参考库、校准、误差分析，不得进入线上推理 |
| 数据集捷径全部禁止 | 数据集名、Image ID、文件名、MOS 均不得输入 VLM |

### 1.3 评测指标

- **SRCC**（斯皮尔曼秩相关）：衡量排名一致性，越高越好，范围 [-1, 1]
- **MAE**（平均绝对误差）：衡量分数精度，越低越好

**注意**：校准操作（Isotonic Regression 等）只影响 MAE，对 SRCC 无贡献。提升 SRCC 必须改善排名质量，必须依赖更好的感知推理。

---

## 2. 已有方法的核心洞察

### 2.1 开源 VLM 零样本 IQA 的根本性局限

文献 [2403.10854] 明确指出：**开源 MLLM 在零样本 IQA 上基本失败**，即使 GPT-4V 也对细粒度质量差异（噪声、压缩伪影）不敏感。根本原因：

> VLM 的视觉表征 $z_v$ 对高层语义高度敏感，对低层感知退化（噪声、模糊、压缩伪影）几乎无响应。——IQA-T1

这意味着**纯 CoT Prompt 方案注定表现差**，必须引入额外感知信号。

### 2.2 得分坍缩（Discrete Collapse）问题

推理型 VLM（Q-Insight、VisualQuality-R1 等）存在明显的**得分坍缩**现象：大量图像被预测为少数几个离散值（如 3.0、4.0、5.0），导致 SRCC 严重下降。原因是 VLM 预训练目标是离散 token 生成，强迫数值预测时倾向于"文本显著数字"。——ME-IQA

### 2.3 三条有效路径

| 路径 | 代表论文 | 核心机制 | 关键约束兼容性 |
|------|---------|----------|----------------|
| **工具增强感知证据** | IQA-T1 | 调用噪声残差图、梯度图、频谱等工具，将客观感知信号注入推理链 | ✅ 工具本身不在评测集上训练 |
| **Agent 任务分解** | AgenticIQA | Planner→Executor→Summarizer，将 IQA 拆为失真检测→失真分析→工具选择→工具执行 | ✅ Planner/Executor 逻辑由 Prompt 驱动 |
| ~~**记忆增强重排序**~~ | ~~ME-IQA~~ | ~~构建参考图记忆库，Thurstone 模型融合相对偏好概率~~ | ❌ 违反 §4.2：仅允许通过 Skill（Prompt）注入规则，外部图片库不属于 Skill |

**注**：ME-IQA 方案已从实现范围中移除。§4.2 明确限定"仅允许通过 Skill（Prompt）注入"，外部检索库无论数据来源如何均不合规。SRCC 提升依靠 Prompt 工程、工具信号优化和 Router 权重调优。

---

## 3. 系统架构

### 3.1 整体流程

```
Image Input
    │
    ▼
┌─────────────────────────────────────────┐
│ Stage 1: Planner（Router 层）           │
│  · 判断 NR-IQA 还是 Other              │
│  · 识别失真类型（Inferred / Explicit）  │
│  · 确定评估范围（Global / Local）       │
│  · 生成结构化执行计划（JSON）           │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│ Stage 2: Executor（感知证据收集）        │
│                                         │
│  Task 1: 失真检测                       │
│    └─ VLM 识别：Blur/Noise/Compression/ │
│       Exposure/Color/Sharpness/Contrast │
│                                         │
│  Task 2: 失真分析                       │
│    └─ VLM 评估每类失真的严重程度        │
│       none/slight/moderate/severe/extreme│
│                                         │
│  Task 3: 工具选择（Router 层决策）      │
│    └─ 根据失真类型选择 NR-IQA 工具      │
│                                         │
│  Task 4: 工具执行                       │
│    └─ 调用客观感知工具获取量化分数      │
│       · NoiseResidualMap 分析           │
│       · GradientMagnitudeMap 分析       │
│       · BRISQUE / NIQE 等统计特征      │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│ Stage 3: Summarizer（证据融合打分）      │
│  · 综合失真分析 + 工具分数              │
│  · HVS 加权方案生成初始质量分 s̃        │
│  · 反思：证据是否充分？不足则回 Stage 1 │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│ Stage 4: ME-IQA 记忆重排序（可选增强）  │
│  · 检索语义+感知对齐的参考图邻居        │
│  · VLM 作为概率比较器                  │
│  · Thurstone Case V 融合 → 连续分 s*   │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│ Stage 5: 分布校准（离线拟合）           │
│  · 5 参数逻辑函数对齐 VLM 分数到 MOS   │
│  · 仅影响 MAE，不影响 SRCC             │
└─────────────────┬───────────────────────┘
                  │
                  ▼
Final Score（0–100）+ 失真分析说明
```

### 3.2 各模块合规性确认

| 模块 | 是否用到评测数据 | 说明 |
|------|----------------|------|
| Planner Prompt | ❌ 否 | 纯规则 Prompt |
| 失真检测/分析 Prompt | ❌ 否 | VLM 内置知识 |
| 工具选择 Prompt | ❌ 否 | 基于失真类型静态映射 |
| 客观感知工具（BRISQUE/NIQE/梯度图等） | ❌ 否 | 传统信号处理，无训练 |
| 锚点记忆库（ME-IQA AM） | ✅ 离线 MOS 合规使用 | 不将 MOS 传入 VLM |
| 对比记忆库（ME-IQA CM） | ✅ 在线积累，无 MOS | 存储精炼后的分数估计 |
| Thurstone 融合 | ❌ 否 | 数值优化，非模型训练 |
| 5 参数校准 | ✅ 离线 MOS 合规使用 | 仅用于 MAE 对齐 |

---

## 4. 核心模块详细设计

### 4.1 Stage 1：Planner（Router 层）

Planner 是 Router/Decision 层的主要组成部分，负责任务分解和策略生成。

**输入**：图像 + 标准提示  
**输出**：结构化 JSON 执行计划

```json
{
  "query_type": "IQA",
  "reference_mode": "No-Reference",
  "distortion_source": "inferred",
  "distortions": null,
  "plan": {
    "distortion_detection": true,
    "distortion_analysis": true,
    "tool_selection": true,
    "tool_execution": true
  }
}
```

**Planner Prompt 要点**：
- 问题类型判断：IQA（技术退化）vs Other（高层语义）
- 失真来源：explicit（查询中明确提及）vs inferred（需推断）
- NR-IQA 模式（无参考图）

### 4.2 Stage 2：Executor

#### Task 1 — 失真检测

检测范围限于以下 7 类（来自 AgenticIQA）：

```
["Blurs", "Color distortions", "Compression",
 "Noise", "Brightness change", "Sharpness", "Contrast"]
```

**Prompt 设计要点**：
- 仅从上述有限列表中选择
- 全局检测（Global scope）
- 输出 JSON：`{"distortion_set": {"Global": ["Noise", "Blur"]}}`

#### Task 2 — 失真分析

对每种检测到的失真评估严重程度：

```
severity: none / slight / moderate / severe / extreme
```

每类失真输出：类型 + 严重程度 + 简短视觉解释。

#### Task 3 — 工具选择（Router 层决策）

根据失真类型映射最适合的客观工具：

| 失真类型 | 推荐工具 |
|---------|---------|
| Blur | GradientMagnitudeMap、BRISQUE |
| Noise | NoiseResidualMap、BRISQUE |
| Compression | FourierMagnitudeSpectrum、BRISQUE |
| Exposure/Brightness | LuminanceHistogram、NIQE |
| Color | PerceptualColorfulnessMap |
| Sharpness | GradientOrientationCoherenceMap |
| Contrast | NIQE |

Router 层此处可以用离线 MOS 学习最优工具选择权重（任务书 §4.3 允许 Router 层训练），训练数据使用 **KADID-10k** 而非评测数据集。

#### Task 4 — 工具执行

**客观感知工具库**（均为传统信号处理，无需训练）：

| 工具 | 类型 | 输出 |
|------|------|------|
| NoiseResidualMap | 图像减去去噪版本 | 噪声残差可视化 |
| GradientMagnitudeMap | Sobel 梯度幅度图 | 结构清晰度 |
| GradientMagnitudeHistogram | 梯度幅度统计 | 锐度分布 |
| FourierMagnitudeSpectrum | 频域分析 | 周期性伪影 |
| LuminanceHistogram | 亮度直方图 | 曝光分布 |
| PerceptualColorfulnessMap | 颜色丰富度图 | 色彩饱和度 |
| GradientOrientationCoherenceMap | 梯度方向一致性 | 模糊/过平滑检测 |
| BRISQUE | 自然场景统计 | 综合盲质量分 |
| NIQE | 自然图像质量估计 | 无监督质量分 |

**关键**：工具输出的图像（残差图、频谱图等）可以作为**额外视觉证据**输入 VLM 的推理链（IQA-T1 的核心思想），让 VLM 看到客观的感知信号而非依赖内部语义表征。

### 4.3 Stage 3：Summarizer

Summarizer 将失真分析结果和工具分数综合为质量评分。

**HVS 启发式加权方案**（来自 AgenticIQA）：

```python
# 1. 工具分数平均
q_mean = mean([tool_scores])  # 范围 1-5

# 2. 构建高斯权重（模拟 HVS 非线性灵敏度）
levels = [1, 2, 3, 4, 5]
weights = softmax([-（q_mean - c)^2 for c in levels])

# 3. VLM 输出各质量等级的 log-prob
log_probs = vlm_logprobs(["bad","poor","fair","good","excellent"])
probs = softmax(log_probs)

# 4. 最终分数
final_score = sum(w * p * c for w, p, c in zip(weights, probs, levels))
```

**反思机制**：若 Summarizer 判断证据不足，触发 Planner 重新规划（最多 1 次循环）。

### 4.4 Stage 4：ME-IQA 记忆增强重排序（可选，提升 SRCC 的关键）

这是解决**得分坍缩**、提升 SRCC 的最有效手段。

#### 4.4.1 记忆库构建（离线，使用 MOS 合规）

**锚点记忆库（AM）**：
- 离线使用 **KADID-10k**（与 KonIQ/SPAQ 无交集的第三方数据集）图像 + MOS 标签构建
- 每张图存储：图像、推理摘要文本、精炼质量分
- GT 分层采样：将 MOS 分成 5 个区间，每区间均匀采样
- 合规性：MOS 仅用于构建库，不在线上传入 VLM；AM 来源数据集与评测集完全隔离，无数据泄露

**对比记忆库（CM）**：
- 在线推理时动态积累
- 存储已处理图像的推理摘要 + 精炼分数估计
- 定期剪枝（保持规模 ≤ C）

#### 4.4.2 检索

用 **推理摘要文本** 作为检索 key（优于原始图像特征）：
- 将 Summarizer 生成的推理文本压缩为简洁质量描述
- 使用文本嵌入模型（如 Qwen3-Embedding-0.6B）编码
- cosine 相似度检索 K=32 个最近邻（AM 贡献 16，CM 贡献 16）

#### 4.4.3 Thurstone Case V 重排序

```
对检索到的每个邻居 j ∈ N：
  1. VLM 比较：query vs neighbor_j，输出 token "A" 的概率 y_ij
  2. 转化为伪观测：μ_ij = sj + Φ⁻¹(y_ij)

闭式解：
  s* = (Σ_j μ_ij + λ·s̃) / (K + λ)

其中 s̃ 是 Stage 3 的初始分数，λ=0.01 为先验强度
```

这将 VLM 从"绝对打分器"转变为"相对比较器"，显著提升 SRCC。

### 4.5 Stage 5：分布校准

**目标**：降低 MAE，对 SRCC 无影响。

使用 5 参数单调逻辑函数（IQA 领域标准做法）：

```python
from scipy.optimize import curve_fit

def five_param_logistic(x, b1, b2, b3, b4, b5):
    return b1 * (0.5 - 1/(1 + np.exp(b2*(x - b3)))) + b4*x + b5

# 离线拟合：在 KADID-10k 上学习 VLM 分数到 MOS 尺度的映射
# 不在 KonIQ/SPAQ 评测集上拟合，避免测试集信息泄露到 MAE 指标
params, _ = curve_fit(five_param_logistic, raw_vlm_scores_kadid, mos_scores_kadid)
calibrated = five_param_logistic(test_scores, *params)
```

---

## 5. Skill（Prompt）模板

### 5.1 Planner Prompt

```
You are a planner in an image quality assessment (IQA) system.
Analyze the image and generate a structured assessment plan.

Return ONLY valid JSON:
{
  "query_type": "IQA",
  "reference_mode": "No-Reference",
  "distortion_source": "inferred",
  "plan": {
    "distortion_detection": true,
    "distortion_analysis": true,
    "tool_selection": true,
    "tool_execution": true
  }
}
```

### 5.2 失真检测 Prompt

```
You are an expert in image distortion detection.
Identify all visible distortions in this image.

Only use distortion types from this list:
["Blurs", "Color distortions", "Compression",
 "Noise", "Brightness change", "Sharpness", "Contrast"]

Return ONLY valid JSON:
{
  "distortion_set": {
    "Global": ["<distortion_1>", "<distortion_2>"]
  }
}
```

### 5.3 失真分析 Prompt

```
You are a distortion analysis expert.
Assess the severity and perceptual impact of each distortion.

Severity scale: none / slight / moderate / severe / extreme

Detected distortions: {distortion_set}

Return ONLY valid JSON:
{
  "distortion_analysis": {
    "Global": [
      {
        "type": "<distortion_type>",
        "severity": "<level>",
        "explanation": "<brief visual evidence>"
      }
    ]
  }
}
```

### 5.4 证据推理打分 Prompt（含工具证据图像）

```
You are a professional image quality assessment expert.
Base your analysis on the following visual evidence:

[Visual Evidence 1: <NoiseResidualMap>]
[Visual Evidence 2: <GradientMagnitudeMap>]
[Tool Score: BRISQUE={score}, NIQE={score}]
[Distortion Analysis: {summary}]

Rate the overall quality on a scale of 1 (very poor) to 5 (excellent).

Score definitions:
1: Very poor — severe degradation, content barely discernible
2: Poor — significant quality issues, distracting to most viewers
3: Fair — some visible issues, acceptable for casual/web use
4: Good — minor issues, suitable for most professional purposes
5: Excellent — minimal or no visible issues, professional quality

Think step by step, reference the visual evidence explicitly.
Return ONLY valid JSON:
{
  "quality_score": <float 1-5>,
  "primary_distortion": "<str>",
  "reasoning": "<evidence-grounded explanation>"
}
```

### 5.5 VLM 比较器 Prompt（ME-IQA 重排序用）

```
You are a perceptual quality comparator.
Compare the quality of Image A (query) and Image B (reference).

Image A: <query_image>
Image B: <reference_image> (reference quality: {ref_quality_description})

Which image has better overall perceptual quality?
Answer with a single letter: A or B.
```

---

## 6. 评估指标与目标

```python
import numpy as np
from scipy.stats import spearmanr

def evaluate(pred_scores, mos_scores):
    srcc, _ = spearmanr(pred_scores, mos_scores)
    mae = np.mean(np.abs(np.array(pred_scores) - np.array(mos_scores)))
    return {"SRCC": srcc, "MAE": mae}
```

### 参考性能基准（来自文献）

| 方法 | KonIQ SRCC | SPAQ SRCC | 备注 |
|------|-----------|-----------|------|
| 纯 CoT 零样本（开源 VLM） | ~0.40 | ~0.45 | 实际差于预期 |
| Dog-IQA（标准引导零样本） | ~0.65 | ~0.70 | 训练无关 SOTA（2024） |
| AgenticIQA（Qwen2.5-VL） | 0.778 | — | TID/BID/AGIQA 验证 |
| Q-Insight（RL 微调）| 0.895 | 0.903 | 有微调，参考上限 |
| IQA-T1（工具+SFT+RL） | 0.925 | 0.908 | 有微调，参考上限 |
| **本方案目标** | **≥ 0.72** | **≥ 0.72** | 训练无关约束下 |

---

## 7. 技术栈

| 组件 | 选型 | 说明 |
|------|------|------|
| VLM 推理 | `transformers` + Qwen2.5-VL-7B-Instruct | 本地或 DashScope API |
| 客观感知工具 | `opencv-python`, `numpy`, `scipy` | 梯度图、残差图、频谱 |
| 传统 IQA 工具 | `piq`, `IQA-PyTorch` | BRISQUE、NIQE 等 |
| 文本嵌入（检索） | Qwen3-Embedding-0.6B 或 sentence-transformers | ME-IQA 检索 key |
| 向量检索 | `faiss-cpu` | 记忆库检索 |
| 校准 | `scipy.optimize.curve_fit` | 5 参数逻辑函数 |
| 评估 | `scipy.stats.spearmanr`, `numpy` | SRCC / MAE |
| 数据处理 | `Pillow`, `pandas` | 图像加载、标签管理 |

---

## 8. 项目目录结构

```
Agent/
├── config/
│   ├── model_config.yaml           # VLM 路径/API、推理参数
│   └── tool_config.yaml            # 工具映射配置
│
├── skills/                         # Skill（Prompt）模块
│   ├── prompts.py                  # 所有 Prompt 模板
│   ├── planner.py                  # Stage 1: Planner
│   ├── executor.py                 # Stage 2: Executor（4 个 Task）
│   └── summarizer.py              # Stage 3: Summarizer
│
├── tools/                          # 客观感知工具库
│   ├── perceptual_tools.py         # NoiseResidualMap/GradientMap 等
│   └── iqa_tools.py               # BRISQUE、NIQE 等传统指标
│
├── router/                         # Router/Decision 层
│   ├── tool_selector.py            # Task 3：工具选择逻辑
│   └── conflict_resolver.py        # 冲突裁决
│
├── memory/                         # ME-IQA 记忆库
│   ├── anchor_memory.py            # 锚点记忆库（AM）构建与检索
│   ├── contrast_memory.py          # 对比记忆库（CM）在线更新
│   └── reranker.py                 # Thurstone Case V 重排序
│
├── calibration/
│   ├── five_param_logistic.py      # 5 参数逻辑函数校准
│   └── fit_calibrator.py           # 离线拟合脚本
│
├── evaluation/
│   ├── metrics.py                  # SRCC / MAE 计算
│   └── run_eval.py                 # 批量评测入口
│
├── data/
│   ├── koniq10k/                   # KonIQ-10k 数据（图像 + MOS CSV）
│   └── spaq/                       # SPAQ 数据
│
├── pipeline.py                     # 主推理 Pipeline
├── build_memory.py                 # 离线构建 AM 脚本
└── requirements.txt
```

---

## 9. 实现优先级

| 优先级 | 模块 | 预期 SRCC 贡献 | 说明 |
|--------|------|---------------|------|
| ★★★ | Executor + 客观感知工具证据注入 VLM | +0.15~+0.20 | 解决 VLM 语义偏差根本问题 |
| ★★★ | ME-IQA 记忆重排序 | +0.08~+0.12 | 解决得分坍缩，直接提升 SRCC |
| ★★ | Planner 任务分解 + 结构化 Prompt | +0.05~+0.08 | 比纯 CoT 显著更好 |
| ★★ | 5 参数逻辑函数校准 | MAE -20~-30% | 仅影响 MAE |
| ★ | Router 工具选择优化（离线学习权重） | +0.02~+0.05 | 任务书允许的 Router 层训练 |
| ★ | Summarizer 反思循环 | 稳健性 | 极端案例改善 |

---

## 10. 与旧版方案的关键区别

| 维度 | v1（旧方案） | v2（本方案） |
|------|------------|------------|
| 核心假设 | 纯 CoT 足够 | VLM 语义偏差是根本问题，需外部感知信号 |
| 评分机制 | 直接输出数字 | 工具证据→推理→VLM 输出质量等级 log-prob |
| 多 Expert 结构 | 并行 3 个 CoT | 顺序 4-Task Executor（失真检测→分析→工具选→执行） |
| 得分坍缩处理 | 未考虑 | ME-IQA Thurstone 重排序 |
| 校准作用 | 误以为提升 SRCC | 明确只降 MAE，SRCC 提升靠感知推理质量 |
| 参考图机制 | 无 | ME-IQA 记忆库（AM+CM）提供局部序数约束 |
| 感知工具 | 无 | 15 类客观工具（噪声残差图、梯度图、频谱等） |
